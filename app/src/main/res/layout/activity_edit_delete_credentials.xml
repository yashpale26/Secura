<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="?android:colorBackground"
    tools:context=".settingsscreen.EditDeleteCredentialsActivity">

    <!-- Toolbar -->
    <androidx.appcompat.widget.Toolbar
        android:id="@+id/toolbar"
        android:layout_width="match_parent"
        android:layout_height="?attr/actionBarSize"
        android:background="@color/blue_standard"
        app:navigationIcon="@drawable/ic_arrow_back"
        app:titleTextColor="?android:textColorPrimary"
        app:subtitleTextColor="?android:textColorSecondary"
        app:popupTheme="@style/Theme.Secura.PopupOverlay"
        app:layout_scrollFlags="scroll|enterAlways">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Manage Credentials"
            android:textColor="@color/white"
            android:textSize="20sp"
            android:fontFamily="sans-serif-medium"
            android:layout_gravity="center_horizontal"/>
    </androidx.appcompat.widget.Toolbar>

    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:padding="16dp">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="20dp"
            android:orientation="vertical">

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Select accounts to manage:"
                android:textSize="18sp"
                android:textStyle="bold"
                android:textColor="@color/text_primary_dark_blue"
                android:layout_marginBottom="16dp"/>

            <!-- RecyclerView to display selectable user credentials -->
            <androidx.recyclerview.widget.RecyclerView
                android:id="@+id/recycler_view_selectable_credentials"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="10dp"
                android:nestedScrollingEnabled="false"
                tools:listitem="@layout/item_selectable_user"/>

            <Button
                android:id="@+id/btn_edit_selected"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="Edit Selected Account"
                style="@style/Widget.App.Button.Primary"
                android:layout_marginTop="24dp"
                android:layout_marginBottom="16dp"
                android:enabled="false"/> <!-- Initially disabled -->

            <Button
                android:id="@+id/btn_delete_selected"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="Delete Selected Accounts"
                style="@style/Widget.App.Button.Outlined"
                android:textColor="@color/error_red"
                app:strokeColor="@color/error_red"
                android:enabled="false"/> <!-- Initially disabled -->

        </LinearLayout>
    </ScrollView>
</LinearLayout>
